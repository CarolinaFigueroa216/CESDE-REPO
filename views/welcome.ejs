<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Bienvenido - CESDE</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet"/>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css"/>
  <link rel="stylesheet" href="/css/welcome.css"/>
  <link rel="stylesheet" href="/css/footer.css"/>
</head>
<body>
  <!-- Contenedor Principal -->
  <div class="welcome-container">
    <!-- Tarjeta de Bienvenida -->
    <div class="welcome-card">
      <!-- Header -->
      <div class="welcome-header">
        <i class="bi bi-check-circle-fill welcome-icon"></i>
        <h1 class="welcome-title">¡Bienvenido!</h1>
      </div>

      <!-- Información del Usuario -->
      <div class="user-info">
        <p class="user-name">
          <i class="bi bi-person-circle me-2"></i>
          <%= user.nombres_y_apellidos %>
        </p>
        <p class="user-email">
          <i class="bi bi-envelope me-2"></i>
          <%= user.correo_electronico %>
        </p>
        <p class="user-role">
          <i class="bi bi-shield-check me-2"></i>
          <% if (user.rol_usuario_superadministrador) { %>
            <span class="badge bg-danger">SuperAdmin</span>
          <% } else if (user.rol_usuario_administrador) { %>
            <span class="badge bg-warning text-dark">Administrador</span>
          <% } else { %>
            <span class="badge bg-primary">Usuario</span>
          <% } %>
        </p>
      </div>

      <!-- Mensaje de Bienvenida -->
      <div class="welcome-message">
        <p>Has iniciado sesión correctamente en el sistema académico CESDE.</p>
        <p>Desde aquí puedes acceder a todas las funcionalidades disponibles para tu rol.</p>
      </div>

      <!-- Acciones Principales -->
      <div class="welcome-actions">
        <a href="/dashboard" class="btn btn-cesde btn-lg mb-2">
          <i class="bi bi-speedometer2 me-2"></i>Ir al Dashboard
        </a>

        <% if (user.rol_usuario_administrador || user.rol_usuario_superadministrador) { %>
          <a href="/admin" class="btn btn-outline-warning btn-lg mb-2">
            <i class="bi bi-shield-check me-2"></i>Panel de Admin
          </a>
        <% } %>

        <% if (user.rol_usuario_superadministrador) { %>
          <a href="/superadmin" class="btn btn-outline-danger btn-lg mb-2">
            <i class="bi bi-star me-2"></i>SuperAdmin
          </a>
        <% } %>

        <a href="/proyectos" class="btn btn-outline-info btn-lg mb-2">
          <i class="bi bi-folder me-2"></i>Mis Proyectos
        </a>

        <a href="/licencias" class="btn btn-outline-success btn-lg mb-2">
          <i class="bi bi-file-earmark me-2"></i>Licencias CC
        </a>
      </div>

      <!-- Información Adicional -->
      <div class="welcome-info">
        <div class="info-box">
          <i class="bi bi-info-circle"></i>
          <p>
            <strong>Nota:</strong> Si necesitas ayuda o tienes dudas sobre el sistema, 
            <a href="mailto:soporte@cesde.edu.co">contacta con soporte</a>.
          </p>
        </div>
      </div>

      <!-- Botón de Logout -->
      <div class="welcome-footer">
        <form method="GET" action="/logout" style="width: 100%;">
          <button type="submit" class="logout-btn btn-block">
            <i class="bi bi-box-arrow-right me-2"></i>Cerrar Sesión
          </button>
        </form>
      </div>
    </div>

    <!-- Información del Sistema -->
    <div class="system-info">
      <p>
        <i class="bi bi-calendar3"></i>
        <span id="currentDate"></span>
      </p>
      <p>
        <i class="bi bi-clock"></i>
        <span id="currentTime"></span>
      </p>
    </div>
  </div>

  <%- include('partials/footer', { user: user }) %>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  <script>
    // Actualizar fecha y hora
    function updateDateTime() {
      const now = new Date();
      document.getElementById('currentDate').textContent = now.toLocaleDateString('es-ES', {
        weekday: 'long',
        year: 'numeric',
        month: 'long',
        day: 'numeric'
      });
      document.getElementById('currentTime').textContent = now.toLocaleTimeString('es-ES');
    }

    updateDateTime();
    setInterval(updateDateTime, 1000);
  </script>
</body>
</html>
