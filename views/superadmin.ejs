<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Panel SuperAdmin - CESDE</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet"/>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css"/>
  <style>
    body {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      min-height: 100vh;
      padding-bottom: 50px;
    }
    .container {
      background: white;
      border-radius: 16px;
      box-shadow: 0 10px 40px rgba(0, 0, 0, 0.15);
    }
    .card {
      transition: all 0.3s ease;
      border-radius: 12px;
      overflow: hidden;
    }
    .card:hover {
      transform: translateY(-8px);
      box-shadow: 0 12px 24px rgba(0, 0, 0, 0.2);
    }
    .card-body {
      padding: 25px;
    }
    .display-4 {
      font-size: 3.5rem;
    }
    .btn {
      border-radius: 8px;
      padding: 8px 20px;
      font-weight: 600;
      transition: all 0.2s ease;
    }
    .btn:hover {
      transform: translateY(-2px);
    }
    .badge {
      padding: 8px 16px;
      font-size: 0.9rem;
      font-weight: 600;
    }
  </style>
</head>
<body>
  <div class="container py-5">
    <!-- Header -->
    <div class="text-center mb-5">
      <i class="bi bi-star-fill display-1 text-danger"></i>
      <h1 class="mt-3 fw-bold">Panel Super Administrador</h1>
      <p class="lead text-muted">Acceso total al sistema CESDE</p>
      <span class="badge bg-danger">Privilegios Máximos</span>
    </div>

    <!-- Cards de acciones -->
    <div class="row g-4 mb-4">
      <!-- Usuarios -->
      <div class="col-md-3">
        <div class="card text-center shadow-sm border-danger border-2">
          <div class="card-body">
            <i class="bi bi-people-fill display-4 text-danger mb-3"></i>
            <h6 class="card-title fw-bold">Usuarios</h6>
            <p class="text-muted small">Gestión CRUD completa</p>
            <a href="/dashboard" class="btn btn-sm btn-danger">
              <i class="bi bi-gear-fill me-1"></i> Gestionar
            </a>
          </div>
        </div>
      </div>

      <!-- Proyectos -->
      <div class="col-md-3">
        <div class="card text-center shadow-sm border-primary border-2">
          <div class="card-body">
            <i class="bi bi-folder-fill display-4 text-primary mb-3"></i>
            <h6 class="card-title fw-bold">Proyectos</h6>
            <p class="text-muted small">Gestionar proyectos estudiantiles</p>
            <a href="/proyectos" class="btn btn-sm btn-primary">
              <i class="bi bi-upload me-1"></i> Ver Proyectos
            </a>
          </div>
        </div>
      </div>

      <!-- Monitor del Sistema -->
      <div class="col-md-3">
        <div class="card text-center shadow-sm border-success border-2">
          <div class="card-body">
            <i class="bi bi-graph-up-arrow display-4 text-success mb-3"></i>
            <h6 class="card-title fw-bold">Monitor</h6>
            <p class="text-muted small">Logs y estadísticas</p>
            <a href="/superadmin/monitor" class="btn btn-sm btn-success">
              <i class="bi bi-speedometer2 me-1"></i> Ver Monitor
            </a>
          </div>
        </div>
      </div>

      <!-- Configuración -->
      <div class="col-md-3">
        <div class="card text-center shadow-sm border-warning border-2">
          <div class="card-body">
            <i class="bi bi-gear-fill display-4 text-warning mb-3"></i>
            <h6 class="card-title fw-bold">Configuración</h6>
            <p class="text-muted small">Ajustes del sistema</p>
            <button class="btn btn-sm btn-warning" disabled>
              <i class="bi bi-wrench me-1"></i> Próximamente
            </button>
          </div>
        </div>
      </div>
    </div>

    <!-- Segunda fila de cards -->
    <div class="row g-4 mb-4">
      <!-- Base de Datos -->
      <div class="col-md-4">
        <div class="card text-center shadow-sm border-info border-2">
          <div class="card-body">
            <i class="bi bi-database-fill display-4 text-info mb-3"></i>
            <h6 class="card-title fw-bold">Base de Datos</h6>
            <p class="text-muted small">Backups y mantenimiento</p>
            <button class="btn btn-sm btn-info" disabled>
              <i class="bi bi-database me-1"></i> Próximamente
            </button>
          </div>
        </div>
      </div>

      <!-- Seguridad -->
      <div class="col-md-4">
        <div class="card text-center shadow-sm border-dark border-2">
          <div class="card-body">
            <i class="bi bi-shield-lock-fill display-4 text-dark mb-3"></i>
            <h6 class="card-title fw-bold">Seguridad</h6>
            <p class="text-muted small">Permisos y auditoría</p>
            <button class="btn btn-sm btn-dark" disabled>
              <i class="bi bi-shield-check me-1"></i> Próximamente
            </button>
          </div>
        </div>
      </div>

      <!-- Reportes -->
      <div class="col-md-4">
        <div class="card text-center shadow-sm border-secondary border-2">
          <div class="card-body">
            <i class="bi bi-file-earmark-bar-graph display-4 text-secondary mb-3"></i>
            <h6 class="card-title fw-bold">Reportes</h6>
            <p class="text-muted small">Análisis y métricas</p>
            <button class="btn btn-sm btn-secondary" disabled>
              <i class="bi bi-file-text me-1"></i> Próximamente
            </button>
          </div>
        </div>
      </div>
    </div>

    <!-- Información del usuario actual -->
    <div class="alert alert-info mb-4">
      <div class="d-flex align-items-center">
        <i class="bi bi-person-circle fs-3 me-3"></i>
        <div>
          <strong>Sesión activa:</strong> <%= user.nombres_y_apellidos %><br>
          <small class="text-muted">ID: <%= user.id_usuarios %> | Email: <%= user.correo_electronico %></small>
        </div>
      </div>
    </div>

    <!-- Advertencia de seguridad -->
    <div class="alert alert-danger" role="alert">
      <i class="bi bi-exclamation-triangle-fill me-2"></i>
      <strong>Advertencia:</strong> Tienes permisos completos sobre el sistema. Usa estas herramientas con responsabilidad. Todas las acciones quedan registradas en los logs del sistema.
    </div>

    <!-- Botones de navegación -->
    <div class="mt-4 text-center">
      <a href="/welcome" class="btn btn-outline-secondary me-2">
        <i class="bi bi-house-fill me-1"></i>Volver al Inicio
      </a>
      <a href="/dashboard" class="btn btn-primary me-2">
        <i class="bi bi-people-fill me-1"></i>Gestión de Usuarios
      </a>
      <a href="/superadmin/monitor" class="btn btn-success me-2">
        <i class="bi bi-graph-up me-1"></i>Monitor del Sistema
      </a>
      <a href="/logout" class="btn btn-outline-danger">
        <i class="bi bi-box-arrow-right me-1"></i>Cerrar Sesión
      </a>
    </div>
  </div>

  <%- include('partials/footer', { user: user }) %>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
